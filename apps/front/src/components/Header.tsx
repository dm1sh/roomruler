import { AppBar, Toolbar, Typography } from "@material-ui/core";

export const Header = () => (
  <AppBar>
    <Toolbar>
      <Typography variant="h6">roomruler</Typography>
    </Toolbar>
  </AppBar>
);
